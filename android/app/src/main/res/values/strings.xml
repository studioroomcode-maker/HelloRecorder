<resources>
    <!--
      앱 이름은 여기 두지 않는다 — build.gradle.kts 의 buildTypes 가 resValue 로 넣는다.
      디버그 빌드는 applicationIdSuffix ".debug" 로 릴리스와 나란히 설치되는데, 이름까지
      같으면 런처에 똑같은 아이콘이 둘 생겨 어느 쪽이 어느 쪽인지 알 수 없다.
    -->
</resources>
