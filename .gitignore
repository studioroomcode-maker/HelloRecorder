# ── Build outputs ──
**/build/
*.apk
*.aab
/android/app/release/
/android/app/debug/
.gradle/
**/.gradle/
.kotlin/
**/.kotlin/
.cxx/
.externalNativeBuild/
/captures/

# ── IDE ──
.idea/
*.iml

# ── Local / SDK config ──
local.properties
**/local.properties

# ── Signing keys — DO NOT COMMIT (public repo) ──
/keystore/
*.jks
*.keystore
keystore.properties
**/keystore.properties

# ── STT 모델 원본 (수백 MB — 기기 push 용, 저장소에 넣지 않음) ──
/models/

# ── OS ──
.DS_Store
Thumbs.db
